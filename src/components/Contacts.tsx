const Contacts = () => {
  return (
    <div
      id="contacts"
      className="min-h-screen flex items-center justify-center"
    >
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;
